<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>
            {% block title %}{% endblock %}
        </title>

        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('style/style.css') }}">
        {% endblock %}

        {% block javascripts %}

        {% endblock %}
    </head>
    <body>
        <nav class="navMenu">
            {% if app.user %}
                <div class="navMenu--links">
                    <a href="{{ path('idea_index') }}">Toutes les idées</a>
                    <a href="{{ path('my_ideas') }}">Mes idées</a>
                </div>
                <div class="flex">
                    <div class="navMenu--connected_name">
                        <img src="{{ asset('images/connected-icon.png') }}" alt="connected" height="15px">
                        <p class="right connected">Connecté avec {{ app.user.email }}</p>
                    </div>
                    <a href="{{ url('app_logout') }}" class="right connected connexion" id="deco">Deconnexion</a>
                </div>
            {% else %}
                <div class="flex">
                    <p class="right">Vous n'êtes pas connecté</p>
                    <a href="{{ path('app_login') }}" class="right connexion">Connexion</a>
                </div>
            {% endif %}
        </nav>
        <main class="main">
            {% block body %}{% endblock %}
        </main>
    </body>
</html>
