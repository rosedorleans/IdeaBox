<form method="post" action="{{ path('idea_delete', {'id': idea.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ idea.id) }}">
    <button class="btn">
        <img src="{{ asset('images/delete.png') }}" alt="delete" onmouseover="this.src='{{ asset('images/delete-fill.png') }}'" onmouseout="this.src='{{ asset('images/delete.png') }}'"/>
    </button>
    </a>
</form>
